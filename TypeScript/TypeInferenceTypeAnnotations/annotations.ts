let username: string = "John"; // We explicitly specify 'string' type
let isActive: boolean = true; // 'boolean' type is assigned manually

function multiply(x: number, y: number): number {
  return x * y; // We explicitly specify return type as 'number'
}
