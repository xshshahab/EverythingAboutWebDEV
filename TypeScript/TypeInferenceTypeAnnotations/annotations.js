var username = "John"; // We explicitly specify 'string' type
var isActive = true; // 'boolean' type is assigned manually
function multiply(x, y) {
    return x * y; // We explicitly specify return type as 'number'
}
